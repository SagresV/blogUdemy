<style type="text/css" media="screen">
	<%= ".#{category.name}"%>{
		background: <%= category.color %> !important;
	}
</style>
<div class="col-md-3">
	<div class="text-center jumbotron <%= "#{category.name}" %>">
		<h3><%= category.name%></h3>
	</div>
</div>

